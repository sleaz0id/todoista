{
  "name": "todoista",
  "private": true,
  "scripts": {
    "build": "cd client && yarn && yarn build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "yarn build && yarn run deploy && echo 'Client built!'"
  },
  "dependencies": {
    "@rails/webpacker": "3.5",
    "babel-preset-react": "^6.24.1",
    "prop-types": "^15.6.2",
    "react": "^16.4.2",
    "react-dom": "^16.4.2"
  },
  "devDependencies": {
    "webpack-dev-server": "2.11.2"
  }
}
